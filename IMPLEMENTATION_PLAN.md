# 페스타 현장 수령 티켓 판매 시스템 구현 계획

## 1. 목표와 성공 기준

이 계획의 최우선 목표는 기능 수가 아니라 주문·결제·재고의 정합성을 증명하는 것이다.

- 재고 2매에 100개 동시 요청을 보내도 점유 성공은 최대 2건이다.
- 동일 사용자는 같은 이벤트에 유효 점유 또는 결제 완료 주문을 최대 1건만 가진다.
- 결제 실패, 점유 만료, 환불 시 재고는 정확히 한 번만 복구된다.
- 결제 성공과 만료, 중복 환불, 동시 입장 같은 경합에서도 하나의 상태 전이만 성공한다.
- 백엔드 인스턴스를 2개 이상 실행해도 동일한 결과를 보장한다.
- `docker compose up --build`와 문서화된 테스트 명령으로 위 결과를 재현할 수 있다.

## 2. 확정하고 시작할 MVP 정책

추가 결정이 없으면 아래 값을 기본 정책으로 사용한다.

| 항목 | 기본값 |
|---|---|
| 이벤트 수 | 데모 이벤트 1개 |
| 초기 재고 | GENERAL 1매, VIP 1매 (환경 변수로 변경 가능) |
| 구매 제한 | 사용자당 이벤트 전체에서 1매 |
| 점유 TTL | 3분 |
| 환불 | 행사 시작 전이며 `VALID`인 미사용 티켓만 가능 |
| 환불 재고 | 환불 성공 시 동일 등급으로 재판매 |
| PG 선택 | `hash(orderId) % 활성 PG 수` 결정적 라우팅, 테스트에서는 PG/결과 고정 |
| PG 실패율 | 기본 5%, 결정적 테스트 모드 제공 |
| 대기열 | 이벤트별 FIFO, 활성 토큰 5분, 활성화 후 주문 생성 제한 2분 |

정책이 변경되더라도 도메인 불변식과 테스트 구조는 유지하고 설정 및 정책 테스트만 조정한다.

### 2.1 설계 인터뷰에서 확정한 운영 정책

- Kafka는 결제 완료 이후 티켓 발급·알림·통계에만 사용한다. PG 승인은 대기열과 PG별 동시 호출 제한으로 보호한 동기 HTTP 호출로 유지한다.
- PG 호출 전 주문과 점유를 `PAYMENT_PROCESSING`으로 조건부 전이한다. 이 상태는 일반 점유 만료 작업에서 제외하며, 결제 결과가 불명확한 수량은 판매 가능한 재고로 노출하지 않는다.
- PG 조회 조정은 최초 요청 후 5초, 15초, 30초, 60초에 자동 수행한다. 60초 후에도 불명확하면 `PAYMENT_REVIEW_REQUIRED`로 전이하고 1분 간격으로 계속 조회하며, 10분부터 운영 경고를 `CRITICAL`로 승격한다. `DECLINED`, `NOT_FOUND_FINAL` 또는 PG 취소 확정 전에는 재고를 복구하지 않는다.
- 서버가 PG 호출 전에 종료되어 `NOT_FOUND_FINAL`이고 결제 처리 기한이 남았다면 동일 `attempt_key`로 승인을 한 번만 자동 재전송한다.
- 결제 API의 멱등성 키는 클라이언트가 보내지만 PG `attempt_key`는 서버가 생성한다. 하나의 주문에서 단 하나의 활성 결제 시도만 허용하고, `UNKNOWN` 또는 `REVIEW_REQUIRED`가 해소되기 전에는 새 시도를 만들지 않는다.
- 사용자 원인 결제 실패는 주문당 최대 3회, 시도 간격은 최소 2초로 제한한다. PG 점검 같은 시스템 원인 실패는 횟수에서 제외한다. 활성 토큰이 유효하면 같은 주문에서 재시도하고, 만료됐으면 대기열에 다시 진입한다. 토큰 만료 시각은 재시도로 연장하지 않는다.
- 한 사용자는 한 이벤트에서 동시에 유효한 구매 권리만 1개 가질 수 있다. 환불이 최종 확정되면 구매 권리를 해제하고 대기열 맨 뒤에서 재구매할 수 있다.
- 환불 요청 시점에 행사 시작 전 자격을 확정하고 주문을 `REFUND_PROCESSING`으로 전이해 입장을 즉시 차단한다. `PAID`로 결제됐지만 아직 티켓이 발급되지 않은 주문도 환불할 수 있다. 환불 성공 후에만 구매 권리와 재고를 복구한다.
- FIFO는 구매 화면에 들어갈 기회가 활성화되는 순서만 보장한다. 활성 사용자 사이의 재고 배정은 PostgreSQL에 점유 요청이 도착한 순서로 결정한다.
- 사용자·이벤트당 대기열 항목은 하나이며 반복 진입은 기존 순번을 반환한다. 판매 종료 전까지 매진·취소표 대기열을 유지하고 재고가 복구되면 기존 FIFO 순서대로 승격을 재개한다.
- Redis 유실 시 대기열과 활성 토큰은 무효화해 재진입시키되 PostgreSQL의 기존 주문·점유·결제·티켓은 보존한다.
- 정합성에 영향을 주는 판매·만료·환불 시간 판정은 PostgreSQL `CURRENT_TIMESTAMP`를 기준으로 한다.
- 관리자 데이터 리셋 API는 만들지 않는다. 개발·시연 환경에서 서비스를 중지한 뒤 PostgreSQL, Redis, Kafka, Mock PG를 초기화하고 seed를 재적용하는 CLI 절차만 제공한다.
- 가장 오래된 미처리 `OrderPaid` Outbox가 30초 이상 적체되면 시스템을 `DEGRADED`로 표시하고 신규 결제 시작을 차단한다. 이미 시작한 결제는 끝까지 반영하며 적체 해소 후 자동 재개한다.
- 행사 시작 시점까지 유효 티켓이 발급되지 않은 `PAID` 주문은 현장 예외 입장을 허용하지 않는다. 주문을 `REFUND_PROCESSING`으로 전이해 전액 자동 환불하고, 늦게 도착한 `OrderPaid` 소비자는 티켓을 발급하지 않는다. 이 상황은 즉시 `CRITICAL`로 경고한다.
- PG 승인 금액 또는 통화가 주문 스냅샷과 다르면 `PAYMENT_REVIEW_REQUIRED`로 격리하고 티켓 발급과 재고 복구를 모두 보류한다. 재조회 후에도 불일치하면 `CRITICAL`로 경고하며, PG 취소 또는 환불 확정 후에만 재고를 복구한다.
- 사용자 원인 결제 실패 3회를 모두 소진하면 주문을 최종 `PAYMENT_FAILED`로 종료하고 점유·재고·구매 권리를 해제한다. 재구매는 대기열 맨 뒤에서 새 주문으로 시작한다. 시스템 원인 실패와 `UNKNOWN`, `PAYMENT_REVIEW_REQUIRED`는 한도에 포함하지 않는다.
- 판매 종료 전까지만 새 주문과 새 결제 시도를 시작할 수 있다. 종료 전에 `PAYMENT_PROCESSING`으로 진입한 결제는 끝까지 확정·조정하며, 뒤늦게 확인된 성공도 유효한 결제로 인정한다. 종료 후 명확히 실패한 결제는 재시도할 수 없고 미시작 점유와 재점유 대기 주문은 종료한다.
- 구매 권리는 `purchase_rights`의 `(user_id, event_id)` 단일 행으로 관리한다. 행을 삭제하지 않고 `AVAILABLE`, `HELD`, `PURCHASED`, `REFUND_PROCESSING` 상태와 소유 `order_id`를 조건부 갱신해 오래된 작업이 새 주문의 권리를 해제하지 못하게 한다.
- Outbox Relay는 lease 기반 선점 후 Kafka에 at-least-once로 발행한다. 발행과 완료 기록 사이의 종료로 생긴 중복은 허용하고, 소비자는 `(consumer_name, event_id)` 유니크 제약과 도메인 변경을 한 트랜잭션으로 처리한다. 이벤트는 `order_id`로 파티셔닝하고 주문별 `aggregate_sequence`를 검증한다.
- 고객 티켓 화면은 고정 입장 코드를 노출하지 않고 60초짜리 서명된 동적 QR 토큰을 발급한다. 입장 시 서명·만료·이벤트를 검증한 뒤 기존 티켓의 `VALID -> USED` 전이를 원자적으로 수행한다. QR 토큰, 티켓 코드, 이메일, 전화번호와 결제·환불·입장 요청 본문은 로그에 남기지 않는다.
- PG별 동시 호출 제한은 PostgreSQL lease 기반 분산 세마포어로 강제한다. 슬롯을 얻은 뒤에만 `PAYMENT_PROCESSING`으로 전이하며, 슬롯 부족은 결제 실패 횟수에 포함하지 않고 토큰·점유 만료 시각도 연장하지 않는다. 승인·조회·환불이 같은 PG 한도를 공유한다.
- 이벤트별 활성 구매자 수는 유효한 활성 토큰 수로 정의한다. 주문 생성 후에도 결제 성공, 최종 실패, 취소 또는 토큰 만료까지 슬롯을 유지하고 터미널 상태에서 즉시 반납한다. 매진 시 승격을 중단하고 재고 복구 후 Redis Lua 스크립트로 원자적으로 재개한다.
- Redis 유실 후 재진입한 사용자는 PostgreSQL의 기존 구매 권리와 주문을 우선한다. 재시도 가능한 주문은 재승격 후 새로 만들지 않고 재개하며, 불명확한 결제는 신규 시도를 차단한다. `PAID`, `COMPLETED`, `REFUND_PROCESSING` 사용자는 대기열 대신 현재 주문 또는 티켓으로 안내한다.
- 결제를 시작하지 않은 점유가 TTL을 넘으면 주문을 `EXPIRED`로 종료하고 재고·구매 권리·활성 토큰을 원자적으로 해제한다. 만료 주문은 되살리지 않으며 재구매는 대기열 맨 뒤에서 새 주문으로 시작한다.
- 결제 시도 생성 시 선택한 PG와 라우팅 설정 버전을 영구 저장한다. 승인 재전송과 조회 조정은 같은 PG만 사용하고, 명확한 실패 후 새 시도를 만들 때만 현재 활성 PG 목록에서 다시 선택할 수 있다. 환불은 실제 승인을 처리한 PG로 요청한다.
- MVP 지원 통화는 `KRW` 하나이며 할인·쿠폰·세금·부분 환불은 제외한다. 금액은 Java `BigDecimal`로 처리하되 scale은 0으로 강제하고, PostgreSQL `NUMERIC(19,0)`과 API/Kafka의 정수 최소 단위 금액을 사용한다. PG 승인·환불의 금액과 통화는 주문 스냅샷과 정확히 대조한다.

## 3. 권장 저장소 구조

```text
.
├── backend/                  # Java 21, Spring Boot 3, Gradle
│   ├── src/main/java/...     # domain, application, infrastructure, api
│   ├── src/main/resources/   # Flyway, application 설정
│   └── src/test/...          # unit, integration, concurrency tests
├── mock-pg/                  # 독립 Spring Boot Mock PG 서버
│   ├── src/main/java/...     # 승인, 조회, 환불, 장애 시나리오 API
│   └── src/test/...
├── frontend/                 # React, TypeScript, Vite
│   ├── src/
│   └── e2e/                  # Playwright 핵심 흐름
├── scripts/                  # 동시성/부하 검증 및 결과 요약
├── compose.yaml              # PostgreSQL, Redis, Kafka 포함
├── .env.example
└── README.md
```

백엔드는 과도한 계층화를 피하고 다음 네 경계만 유지한다.

- `domain`: 상태와 상태 전이 규칙
- `application`: 유스케이스와 짧은 트랜잭션 경계
- `infrastructure`: PostgreSQL, Redis, Kafka, 외부 Mock PG HTTP 어댑터
- `api`: 인증, 요청 검증, 오류 응답, OpenAPI

## 4. 핵심 설계 결정

### 4.1 진실 공급원

- PostgreSQL: 이벤트, 재고, 주문, 점유, 결제, 티켓, 입장, 감사 이력
- Redis: 대기 순번과 만료 가능한 구매 접근 토큰만 저장
- Kafka: 결제 완료 이후 티켓 발급, 알림, 운영 통계 같은 비동기 후처리 전달
- 프로세스 메모리: 캐시 또는 Mock 동작 제어에만 사용하며 정합성 판단에는 사용하지 않음

Kafka 자체를 진실 공급원으로 보지 않는다. 주문·재고·티켓의 최종 상태는 항상 PostgreSQL에서 조회하며, Kafka 장애 중에도 결제 결과와 재고 반영은 커밋될 수 있어야 한다.

### 4.2 재고 모델과 불변식

`inventories`에 `total`, `available`, `held`, `sold`, `version`을 저장한다.

```text
available + held + sold = total
available >= 0, held >= 0, sold >= 0
```

점유 획득은 `available > 0` 조건의 단일 `UPDATE`로 수행한다. 주문 생성, 점유 생성, 재고 갱신, 상태 이력 추가는 같은 짧은 DB 트랜잭션에 둔다. PG 네트워크 호출은 커밋 후 실행한다.

### 4.3 중복 방지 장치

- 주문 멱등성: `(user_id, idempotency_key)` 유니크 + 요청 본문 해시
- 사용자 구매 제한: 이벤트 기준 활성 주문을 제한하는 DB 제약 또는 별도 구매 권리 행 잠금
- 점유 해제: 점유 상태를 조건부 전이한 트랜잭션에서만 재고 복구
- 결제 시도: `(order_id, attempt_key)` 유니크
- Outbox 이벤트: 이벤트 ID 유니크, 발행 완료 시각 기록
- Kafka 소비: `(consumer_name, event_id)` 유니크로 소비자 멱등성 보장
- 티켓: 주문당 1개, 티켓 코드 유니크
- 입장: 티켓당 1개 admission 유니크 + `VALID -> USED` 조건부 갱신
- 환불: 주문당 환불 시도 키 유니크 + 허용 상태 조건부 전이

부분 유니크 인덱스만으로 표현하기 어려운 주문 상태 범위는 이벤트·사용자별 구매 권리 테이블을 두어 원자적으로 선점하는 단순한 방식을 우선 검토한다.

### 4.4 주문 가격 스냅샷

주문 생성 시 서버가 현재 이벤트와 티켓 등급 정보를 조회해 아래 값을 `orders`에 스냅샷으로 저장한다.

- `unit_price`
- `currency`
- `grade_name_snapshot`
- `event_name_snapshot`

클라이언트가 보낸 가격은 신뢰하지 않는다. 결제와 환불은 주문에 고정된 금액과 통화만 사용하며, 주문 생성 이후 이벤트명·등급명·판매 가격이 변경되어도 기존 주문에는 영향을 주지 않는다. 동일 멱등성 키의 요청 본문 해시에는 사용자가 선택한 이벤트와 등급 등 요청 의도를 포함하되, 서버가 결정하는 가격 스냅샷은 클라이언트 입력으로 받지 않는다.

### 4.5 외부 호출과 장애 복구

Mock PG는 메인 백엔드와 프로세스 및 저장소가 분리된 외부 서비스로 실행한다. 메인 백엔드는 Spring `RestClient` 또는 JDK `HttpClient`로 호출한다. 신규 구현에서는 유지보수 모드인 `RestTemplate`보다 `RestClient`를 기본 선택으로 사용한다.

결제 요청은 다음 세 구간으로 분리한다.

1. DB에서 주문과 점유 상태를 확인하고 주문·점유를 `PAYMENT_PROCESSING`, 결제 시도를 `PROCESSING`으로 조건부 전이한다.
2. 트랜잭션 밖에서 독립 Mock PG의 REST API를 호출한다.
3. HTTP 결과를 새 트랜잭션에서 결제 시도와 주문 상태로 먼저 영속화하고, 성공이면 재고·이력·`OrderPaid` Outbox 이벤트를 함께 반영한다.
4. Outbox Relay가 커밋된 결과를 Kafka로 발행해 티켓 발급 등 후속 처리를 비동기로 실행한다.

HTTP 응답을 받은 메모리 상태에서 Kafka에 직접 발행하지 않는다. 그렇게 하면 Kafka 발행 후 DB 커밋 실패 또는 DB 커밋 후 프로세스 종료로 불일치할 수 있다. 타임아웃은 실패로 단정하지 않고 `UNKNOWN`으로 기록한다. 동일 결제 키로 Mock PG 조회 API를 호출해 조정한 뒤에만 재결제를 허용한다.

Mock PG는 다음 API와 결정적 장애 제어를 제공한다.

- `POST /mock-api/payments`: 결제 승인 요청, 결제 시도 키 멱등 처리
- `GET /mock-api/payments/{attemptKey}`: 타임아웃 이후 실제 처리 결과 조회
- `POST /mock-api/refunds`: 환불 요청과 중복 환불 방지
- 응답 지연, 응답 전 연결 종료, 성공 처리 후 응답 유실, 고정 실패 코드 제어
- PG 자체 PostgreSQL 또는 경량 영속 저장소에 처리 결과를 보존해 재시작 후에도 조회 가능

### 4.6 Kafka와 Transactional Outbox

결제 성공 트랜잭션은 주문을 `PAID`로 바꾸고 재고를 판매 상태로 옮기는 동시에 `outbox_events`에 `OrderPaid`를 기록한다. 별도 Outbox Relay가 이 행을 Kafka에 발행하고, 티켓 소비자가 이벤트를 처리해 티켓을 발급한 뒤 주문을 `COMPLETED`로 전이한다.

```text
Backend → REST → Mock PG
  → PostgreSQL: 결제 결과 + PAID + 재고 판매 반영 + OrderPaid Outbox (한 트랜잭션)
  → Outbox Relay → Kafka
  → Ticket Consumer: 티켓 발급 + COMPLETED
```

Kafka는 기본적으로 at-least-once 전달로 취급한다. Kafka의 exactly-once 설정에 도메인 정합성을 의존하지 않고, 이벤트 ID와 DB 유니크 제약으로 소비자를 멱등하게 만든다. 발행 또는 소비 실패는 지수 백오프로 재시도하고 한도 초과 시 DLT로 보내며, 관리자 재처리 수단과 대기 건수 모니터링을 제공한다.

가장 오래된 미처리 `OrderPaid`가 30초를 넘으면 신규 결제 시작을 차단하고, 적체 해소 후 자동으로 재개한다. 이미 시작한 결제는 Kafka 상태와 무관하게 최종 결과와 Outbox까지 기록한다.

Kafka 적용 범위는 다음으로 제한한다.

- 필수: `OrderPaid` 기반 비동기 티켓 발급
- 권장: `TicketIssued`, `OrderRefunded`, `TicketUsed` 기반 사용자 알림과 운영 통계 projection
- 선택: 결제·환불 reconciliation 작업 트리거. 실제 상태 판정은 반드시 PG 조회와 PostgreSQL 조건부 전이로 수행
- 제외: 재고 점유/복구, 구매 제한, 대기열 입장 허가. 즉시 정합성이 필요한 경로는 PostgreSQL/Redis에서 동기 처리

## 5. 단계별 실행 계획

각 단계는 이전 단계의 검증 게이트를 통과해야 완료로 본다.

### 단계 0 — 프로젝트 골격과 실행 환경

구현:

- 모노레포에 메인 Spring Boot, 독립 Mock PG Spring Boot, React/Vite, PostgreSQL, Redis, Kafka 구성
- Docker Compose, 헬스 체크, `.env.example`, 공통 요청 ID 로깅
- Flyway 도입 및 CI에서 백엔드/프런트 기본 테스트 실행
- API 오류 형식 `{ code, message, requestId, details? }` 확정

산출물:

- 실행 가능한 빈 애플리케이션
- `/actuator/health`와 프런트 시작 화면
- 로컬 실행 및 테스트 명령이 있는 README 초안

검증 게이트:

- 깨끗한 환경에서 `docker compose up --build` 성공
- 메인 API, Mock PG, PostgreSQL, Redis, Kafka 헬스 체크 통과

### 단계 1 — 이벤트 카탈로그와 개발용 인증

구현:

- `users`, `events`, `ticket_grades`, `inventories` 스키마와 제약
- 데모 사용자/이벤트/등급/재고 seed
- 개발용 로그인과 역할(`CUSTOMER`, `OPERATOR`, `ADMIN`) 처리
- 이벤트 목록/상세/등급별 잔여 수량 API와 최소 UI

주요 API:

- `POST /api/auth/dev-login`
- `GET /api/events`
- `GET /api/events/{eventId}`

검증 게이트:

- DB 재시작 후에도 seed와 조회 값 일치
- 판매 전/판매 중/판매 종료 이벤트 상태 테스트 통과
- 비로그인·권한 오류가 표준 오류 형식을 사용

### 단계 2 — 원자적 주문 생성과 재고 점유

테스트를 먼저 작성한 뒤 구현한다.

구현:

- `orders`, `reservations`, `order_status_histories` 스키마
- 주문 생성 시 서버 기준 가격·통화·이벤트명·등급명 스냅샷 저장
- 주문 상태 전이 정책과 허용 전이 단위 테스트
- 조건부 재고 차감, 사용자 구매 제한, 주문 생성 멱등성
- 같은 키/같은 본문은 기존 응답, 같은 키/다른 본문은 `409`
- 이 단계에서는 테스트용 구매 허가를 사용하고 Redis 대기열은 아직 연결하지 않음

주요 API:

- `POST /api/orders` (`Idempotency-Key` 필수)
- `GET /api/me/orders`

검증 게이트:

- PostgreSQL Testcontainers에서 재고 2매/동시 요청 100개 중 성공 정확히 2건
- 한 사용자의 동시 요청은 최대 1건 성공
- 모든 실행 후 `available + held + sold = total`
- 멱등 재요청과 키 충돌 테스트 통과
- 주문 생성 후 등급 가격이나 표시명이 변경되어도 기존 주문의 결제·환불 금액과 주문 표시가 바뀌지 않음

### 단계 3 — Mock 결제, 실패, 재시도와 조정

구현:

- 메인 백엔드의 `PaymentGateway` 포트와 HTTP 어댑터
- 독립 Mock PG 서버 및 복수 PG 프로필/라우팅
- `payment_attempts` 및 성공/실패/알 수 없음 상태
- 주문 ID 해시 기반의 결정적 PG 선택과 결정적 결과/지연 제어
- Mock PG의 결제 승인·조회·환불 REST API와 결제 시도 키 멱등성
- HTTP connect/read timeout과 응답 유실 시나리오
- 결제 성공 시 `held - 1`, `sold + 1`; 명확한 실패 시 `held - 1`, `available + 1`
- 실패 사유와 재시도 가능 여부 매핑
- 실패 주문 재시도 시 새 점유를 먼저 원자적으로 획득
- 타임아웃 후 조회 기반 reconciliation
- 오래된 `PAYMENT_PROCESSING` 시도를 `FOR UPDATE SKIP LOCKED`로 자동 조정
- 결제 성공 트랜잭션에 `OrderPaid` Outbox 이벤트를 원자적으로 기록
- 결제 처리/실패/결과 확인/재시도 UI

주요 API:

- `POST /api/orders/{orderId}/payments`
- `POST /api/orders/{orderId}/payments/reconcile`

검증 게이트:

- PG 호출 동안 DB 트랜잭션이 열려 있지 않음을 테스트 또는 로그로 확인
- Mock PG를 실제 HTTP로 호출하는 통합 테스트 통과
- 성공, 명확한 실패, 타임아웃 후 PG 성공/실패 각각의 상태와 재고 검증
- PG가 성공 처리한 뒤 HTTP 응답이 유실되어도 조회 조정으로 최종 `PAID`에 도달
- 같은 결제 시도 키를 반복해도 PG 처리와 재고 변경이 한 번뿐임
- 결제 성공 직후 서버를 종료해도 Outbox 이벤트가 유실되지 않음

### 단계 4 — 점유 만료와 다중 인스턴스 경합

구현:

- 요청 시 지연 정리와 주기적 만료 배치
- `FOR UPDATE SKIP LOCKED` 또는 조건부 상태 갱신으로 작업 분배
- 만료 전이와 재고 복구를 한 트랜잭션에서 수행
- 결제 성공과 만료의 경합은 현재 점유 상태 조건으로 한쪽만 승리
- Compose에서 백엔드 2개 인스턴스를 같은 PostgreSQL/Redis에 연결

검증 게이트:

- 두 만료 작업자가 같은 점유를 읽어도 재고 복구는 한 번
- 만료 직전 결제 성공 경합을 반복 실행해도 불변식 유지
- 프로세스 재시작 후 만료 점유가 정상 복구됨

### 단계 5 — Redis 대기열과 구매 접근 제어

구현:

- 이벤트별 원자적 FIFO 순번 발급
- 활성 사용자 수 제한, 활성 토큰 TTL, 사용자·이벤트 바인딩
- 사용자·이벤트당 단일 대기 항목과 반복 진입 멱등 처리
- 활성 토큰 5분, 활성화 후 주문 생성 제한 2분, 재시도로 만료 시각 연장 금지
- 매진 후에도 판매 종료 전까지 취소표 대기열 유지 및 재고 복구 시 승격 재개
- 주문 API에서 활성 토큰 검증
- 주기적 승격 또는 요청 기반 승격을 멱등하게 구현
- 대기 순번, 상태, 구매 가능 여부 UI

주요 API:

- `POST /api/events/{eventId}/queue`
- `GET /api/queue/{token}`

검증 게이트:

- 동일 이벤트 요청 순서에 따른 순번 발급
- 만료·탈취·다른 사용자의 토큰 재사용 차단
- 백엔드 2개 인스턴스에서도 활성 한도 초과 없음
- Redis 초기화 후 DB 정합성에는 영향 없음

### 단계 6 — Kafka 기반 티켓 발급과 1회 입장

구현:

- Kafka topic, Outbox Relay, 재시도/DLT 정책 구성
- `outbox_events`, `processed_events`, `tickets`, `admissions` 스키마
- `OrderPaid` 소비 후 추측하기 어려운 티켓 코드를 발급하고 `PAID -> COMPLETED` 전이
- 이벤트 중복 수신 시 기존 티켓을 반환하는 멱등 소비 처리
- 발행/소비 지연 중 UI에 `PAID`와 "티켓 발급 중" 상태 표시
- Outbox 적체 30초 이상 시 `DEGRADED` 표시와 신규 결제 차단
- 티켓 소유자/이벤트/기간/상태 검증
- 운영자용 조회 및 원자적 `VALID -> USED` 처리
- `TicketIssued`, `TicketUsed` 이벤트를 알림/운영 통계용으로 발행
- 고객 티켓 코드/상태 UI와 최소 운영 화면

주요 API:

- `GET /api/me/tickets`
- `POST /api/admissions/validate`
- `POST /api/admissions/use`

검증 게이트:

- 결제 성공 주문당 티켓 정확히 1개
- 동일 `OrderPaid` 이벤트를 여러 번 전달해도 티켓과 `COMPLETED` 이력은 한 번만 생성
- Kafka 중단 후 재기동하면 미발행 Outbox가 전달되어 `PAID -> COMPLETED`로 복구
- 소비 실패 메시지가 재시도 후 DLT로 이동하고 관리자 재처리가 가능
- 동일 티켓 동시 입장 50건 중 성공 1건
- 만료·취소·이미 사용된 티켓 차단

### 단계 7 — 환불, 이력과 관리자 기능

구현:

- 환불 시도와 `REFUND_PENDING`, `REFUNDED` 처리
- 환불 접수 시 `REFUND_PROCESSING`으로 전이해 입장을 차단하고 요청 시점의 환불 자격 고정
- 티켓 발급 전 `PAID` 주문 환불과 티켓 발급 Consumer 간 조건부 상태 경합 처리
- PG 환불 성공 후 주문·티켓·재고를 원자적으로 반영
- 환불 성공 트랜잭션에 `OrderRefunded` Outbox 이벤트를 기록하고 알림/통계 projection 갱신
- 불명확한 환불 결과 조회/재처리
- 주문 상태 이력, 결제/실패 통계, 재고 현황 API/UI
- 서비스 중지 후에만 실행하는 개발·시연 전용 데이터 리셋 CLI

주요 API:

- `POST /api/orders/{orderId}/refunds`
- `GET /api/admin/sales`

검증 게이트:

- 중복 환불 50건에도 성공과 재고 증가는 각각 1회
- `USED`, `CANCELLED`, 행사 시작 후 티켓 환불 차단
- 환불 후 생긴 재고로 새 사용자가 구매 가능
- 리셋 후 seed 데이터와 모든 수량 복원

### 단계 8 — E2E, 부하 증거와 제출 마감

구현:

- Playwright 핵심 사용자 흐름 3개
- 동시성 테스트 스크립트와 실행 결과 요약 출력
- OpenAPI 문서와 주요 요청/응답 예시
- README에 실행, 설정, 리셋, 테스트, 설계 결정, 장애 복구, 한계 기록
- 구조화 로그에 request/order/payment/user ID 연결
- Kafka 로그에 event ID, correlation ID, topic, partition, offset을 연결하고 Outbox/DLT 적체 지표 노출

필수 E2E:

1. 로그인 → 대기열 → 주문 → 결제 성공 → 티켓 확인 → 입장
2. 결제 실패 사유 확인 → 재시도 → 성공
3. 결제 성공 → 환불 → 티켓 무효화 → 재고 복구

최종 검증 게이트:

- 새 환경에서 README만 보고 실행·테스트·리셋 가능
- 자동화 테스트 결과와 동시성 요청/성공/실패/최종 재고 스냅샷 보존
- 완료 조건 전 항목을 체크리스트로 확인

## 6. 테스트 전략

### 빠른 피드백 계층

- 단위: 상태 전이, 정책, 실패 코드 매핑, 티켓 유효성
- 저장소 통합: 모든 SQL 조건부 갱신과 DB 제약
- 유스케이스 통합: 주문·결제·만료·환불 트랜잭션
- 외부 연동 통합: 독립 Mock PG 컨테이너로 지연, 타임아웃, 성공 응답 유실과 조회 조정 검증
- 메시징 통합: Kafka Testcontainers로 커밋된 Outbox 발행, 중복 소비, 재시도/DLT 검증
- 경합 테스트: 고정 스레드 풀과 시작 장벽으로 동시 요청 재현
- E2E: 사용자에게 보이는 핵심 세 흐름만 유지

### 성능 검증 목표

- 대기열 등록: 1,000 RPS에서 p95 300ms 이하
- 대기 상태 조회: 2,000 RPS에서 p95 200ms 이하
- 이벤트당 활성 구매자: 기본 100명, 초당 20명 승격
- 주문·재고 점유: p95 500ms 이하
- 동기 PG 결제 확정: p50 1.5초, p95 3초 이하
- 결제 확정 후 티켓 발급: p95 2초 이하
- 인프라 오류율: 1% 미만, 초과 판매: 0건

정합성 시나리오는 재고 2매와 동시 주문 100건으로 검증하고, 성능 시나리오는 재고 10,000매 이상에서 별도로 실행한다. 장시간 안정성은 10분 동안 100 RPS를 유지하며 DB 연결 풀, Redis, Kafka 적체를 확인한다. 측정 환경의 CPU·메모리·컨테이너 수를 결과와 함께 기록한다.

H2 같은 대체 DB로 동시성 정합성을 검증하지 않는다. 관련 테스트는 실제 PostgreSQL Testcontainers를 사용한다. 시간 의존 로직은 주입 가능한 `Clock`을 사용해 sleep 없이 검증한다.

### 매 테스트에서 확인할 공통 불변식

```text
inventory.available + inventory.held + inventory.sold = inventory.total
활성 점유 수 = inventory.held
환불되지 않은 판매 수 = inventory.sold
주문당 성공 결제 <= 1
주문당 활성 티켓 <= 1
티켓당 입장 기록 <= 1
```

## 7. 구현 우선순위와 예상 PR 분할

리뷰 가능한 크기를 유지하기 위해 단계 하나를 거대한 PR로 만들지 않는다.

1. 골격/Compose/CI
2. 이벤트·재고 스키마와 조회
3. 인증과 카탈로그 UI
4. 주문·점유 스키마 및 도메인 규칙
5. 원자적 점유와 동시성 테스트
6. 독립 Mock PG 서버와 HTTP 결제 어댑터
7. 결제 성공/실패, 타임아웃 조정, 재시도와 Outbox 기록
8. 점유 만료 및 다중 인스턴스 검증
9. Redis 대기열과 UI
10. Kafka Outbox Relay와 멱등 티켓 발급
11. 티켓 입장과 후처리 이벤트
12. 환불과 운영 현황
13. E2E·부하 증거·문서 마감

각 PR은 관련 마이그레이션, 테스트, API 문서 변경을 함께 포함하며, 이후 기능을 위한 추측성 추상화는 추가하지 않는다.

## 8. 주요 위험과 대응

| 위험 | 대응 |
|---|---|
| 애플리케이션 선검사 후 갱신으로 초과 판매 | 조건이 포함된 단일 SQL 갱신과 영향 행 수 확인 |
| PG 호출 중 DB 잠금 장기 유지 | 호출 전/후 트랜잭션 분리 |
| PG 성공 후 HTTP 응답 유실 | 결제 시도 키로 PG 조회 후 조건부 상태 조정 |
| HTTP 결과 수신 직후 서버 종료 | 결과 반영과 Outbox 기록을 하나의 DB 트랜잭션으로 커밋 |
| 서버 종료로 PG와 주문 상태 불일치 | 결제 시도 키와 조회 기반 reconciliation |
| 만료 작업자 중복 복구 | 점유 조건부 상태 전이와 같은 트랜잭션의 재고 갱신 |
| 사용자 제한 우회 | DB 수준 원자적 구매 권리 선점 |
| 테스트가 타이밍에 의존 | 주입 가능한 Clock, 시작 장벽, 결정적 Mock 결과 |
| Redis 장애가 재고 손상으로 전파 | Redis를 접근 제어에만 사용하고 재고 판단은 PostgreSQL에서 재검증 |
| DB 커밋 후 Kafka 발행 전 서버 종료 | 같은 DB 트랜잭션의 Transactional Outbox와 재시도 가능한 Relay 사용 |
| Kafka 중복 전달로 티켓 중복 발급 | 이벤트 ID 소비 기록과 주문당 티켓 유니크 제약으로 멱등 처리 |
| Kafka 장애로 티켓 발급 지연 | 주문을 `PAID`로 보존하고 복구 후 재발행, UI에 발급 중 상태 표시 |
| 처리 불가 이벤트 무한 재시도 | 제한된 백오프 재시도, DLT, 알림 및 관리자 재처리 |
| 범위 과대 | 실제 PG, 좌석, 고급 QR 디자인과 핵심 흐름 밖의 이벤트 분리는 제외 |

## 9. 착수 순서

첫 구현 사이클은 단계 0부터 단계 2까지로 잡는다. 이 시점에 화면은 단순해도 좋지만, 실제 PostgreSQL에서 재고 2매 동시성 테스트가 통과해야 한다. 이후 결제와 복구를 붙이고, 대기열은 핵심 DB 정합성이 증명된 다음 연결한다. 이 순서로 진행하면 가장 큰 기술 위험인 초과 판매와 중복 복구를 초기에 확인할 수 있다.
