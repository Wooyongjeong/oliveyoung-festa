# 설계 인터뷰 인계 메모

## 현재 상태

- 작업 브랜치: `plan/phase-0-foundation`
- 구현은 단계 0 골격까지 존재한다.
- `$grill-me`로 `IMPLEMENTATION_PLAN.md`의 정책과 장애 경합을 검토하는 중이다.
- 2026-08-07까지 확정한 상세 정책은 `IMPLEMENTATION_PLAN.md`의 **2.1 설계 인터뷰에서 확정한 운영 정책**에 반영했다.

## 확정한 핵심 방향

- PostgreSQL이 주문·재고·결제·티켓의 진실 공급원이다.
- PG 승인은 대기열과 Bulkhead로 보호한 동기 HTTP 호출로 유지한다.
- Kafka와 Transactional Outbox는 결제 확정 후 티켓 발급·알림·통계에 사용한다.
- 결제 시작 시 `PAYMENT_PROCESSING`으로 전이해 일반 점유 만료와 경합하지 않게 한다.
- 불명확한 PG 결과는 재고를 계속 보류하고 자동 reconciliation으로 확정한다.
- 환불 접수 시 입장을 차단하며, 티켓 발급 전 `PAID` 주문도 환불할 수 있다.
- 대기열 FIFO는 구매 화면 진입 순서만 보장하고 활성 사용자끼리는 DB 도착 순서로 경쟁한다.
- Redis 유실 시 대기열은 재진입시키되 PostgreSQL의 기존 업무 상태는 보존한다.
- 정합성 시간 판정은 PostgreSQL `CURRENT_TIMESTAMP`를 사용한다.
- 개발 데이터 리셋은 서비스 중지형 CLI로만 제공한다.
- 티켓 발급 전에는 현장 예외 입장을 허용하지 않으며, 행사 시작까지 발급되지 않은 `PAID` 주문은 전액 자동 환불한다.
- 승인 금액·통화 불일치는 `PAYMENT_REVIEW_REQUIRED`로 격리하고 PG 취소·환불 확정 전까지 재고를 보류한다.
- 결제 실패 한도 소진, 판매 종료 경계, 미결제 점유 만료와 구매 권리 해제 정책을 확정했다.
- 구매 권리는 사용자·이벤트별 단일 상태 행과 소유 주문 조건으로 보호한다.
- Outbox/Kafka는 lease 기반 at-least-once 전달과 멱등 소비를 사용한다.
- 고객 입장에는 60초 서명 동적 QR을 사용하고 민감 정보와 요청 본문을 로그에서 제외한다.
- PG 호출 제한은 PostgreSQL lease 세마포어로, 대기열 활성 슬롯은 Redis의 유효 활성 토큰 수로 관리한다.
- Redis 유실 뒤에는 기존 PostgreSQL 주문을 우선해 재개하며, 결제 시도별 PG를 고정해 조회·환불 라우팅을 보존한다.
- MVP 통화는 KRW 하나이며 Java `BigDecimal` scale 0, DB `NUMERIC(19,0)`, 외부 계약의 정수 최소 단위 금액을 사용한다.

## 다음 세션 시작 프롬프트

```text
$grill-me
IMPLEMENTATION_PLAN.md와 SESSION_HANDOFF.md를 모두 읽어줘.
이전 세션에서 2.1의 정책까지 확정했어.
이미 확정한 질문은 반복하지 말고, 아직 정의되지 않은 가장 위험한 설계 쟁점부터 한 번에 하나씩 질문해줘.
```

## 다음 세션에서 검토할 후보

- 환불 reconciliation이 행사 종료 후에도 장기간 확정되지 않을 때의 운영 종료·수동 처리 정책
- 동적 QR 서명 키의 보관, 교체와 다중 인스턴스 배포 정책
- 주문·점유·결제·구매 권리의 전체 상태 전이표와 이를 강제할 정확한 DB 제약
- DLT 재처리 권한, 감사 기록과 잘못된 이벤트를 수정·폐기하는 운영 절차
- 활성 구매자 100명과 초당 20명 승격의 환경별 튜닝 및 검증 방법
